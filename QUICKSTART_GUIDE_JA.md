# インストールガイド — Interview Pipeline Tracker

このガイドは、インストールして使うだけの方を対象としています。コーディングやターミナル操作、開発者向けツールは一切不要です。

---

## システム要件

| 要件 | 詳細 |
|-------------|---------|
| OS | Windows 10 または Windows 11（64bit） |
| WebView2 Runtime | 最新の Windows 10/11 端末には標準でインストール済み（Microsoft Edge に同梱） |

これだけです。Node.js、Rust、その他の開発者向けツールは**不要**です。

---

## ステップ1 — インストーラーをダウンロード

このリポジトリの [Releases](../../releases) ページにアクセスし、最新のインストーラーをダウンロードしてください。

- **`Interview Pipeline Tracker_x.x.x_x64-setup.exe`** — 推奨（NSIS セットアップウィザード）
- または **`Interview Pipeline Tracker_x.x.x_x64_en-US.msi`** — 代替の MSI パッケージ

どちらでも動作します。`.exe` のセットアップウィザードが最も簡単な方法です。

---

## ステップ2 — インストーラーを実行

1. ダウンロードした `.exe` ファイルをダブルクリックします
2. Windows の SmartScreen の警告（「Windows によって PC が保護されました」）が表示された場合は、**詳細情報** → **実行** をクリックしてください
   > 有料の証明書でコード署名されていないため、この警告が表示されます。このアプリは安全にインストールできます。
3. 画面の指示に従って進めてください（次へ → インストール → 完了）

---

## ステップ3 — アプリを起動

インストールが完了したら、**スタートメニュー**から **Interview Pipeline Tracker** を見つけて、通常のアプリと同じように起動してください。

---

## アプリの使い方

### 面接の追加
1. 右上の **＋ Add Interview**（面接を追加）をクリックします
2. 企業名、選考ステージ、日時を入力し、必要に応じてリマインダーのタイミングとメモを入力します
3. **Save**（保存）をクリックします

### 選考ステージ
| ステージ | 意味 |
|-------|---------|
| Casual | カジュアル面談 |
| First | 一次面接 |
| Second | 二次面接 |
| Final | 最終面接 |
| Offer | 内定 |
| Rejected | 不採用 |
| Closed | 手動でクローズ／選考辞退 |

### 表示方法
- **リスト表示** — 直近の日時順に並び、カウントダウン表示とステージバッジ付き
- **カレンダー表示** — 月単位のグリッド表示。任意の日をクリックすると、その日の予定の確認・追加・編集ができます

### リマインダー
本アプリはバックグラウンドでリマインダー機能を実行します。`現在時刻 >= scheduled_at（予定日時）− reminder_minutes_before（リマインダー設定分数）` の条件を満たすと、Windows のネイティブ通知が表示されます。**Windows の設定 → システム → 通知** でアプリの通知が有効になっていることを確認してください。

### 編集・削除
各エントリの**鉛筆アイコン**をクリックすると編集、**ゴミ箱アイコン**をクリックすると削除できます。

### 過去の予定
予定日時を過ぎた面接は、自動的にリストの下部にある折りたたみ式の **Past Interviews**（過去の面接）セクションに移動します。

---

## アンインストール

**Windows の設定 → アプリ → インストール済みアプリ** から「Interview Pipeline Tracker」を検索し、**アンインストール**をクリックしてください。

面接データは以下の場所に保存されています。
```
C:\Users\<YourName>\AppData\Local\dev.local.interview-pipeline-tracker\interviews.db
```
このファイルはアンインストール時に**自動的には削除されません**。すべてのデータを削除したい場合は、手動で削除してください。
